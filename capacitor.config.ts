import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Book Rosa Sugar Daddy',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
